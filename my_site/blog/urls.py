from django.urls import path
from . import views

urlpatterns = [
    path("", views.index, name="MDlmY"),
    # path("posts/", views.view_all_posts, name="qAtTF"),
    # path("posts/create/", views.create_post, name="VoeLr"),
    # path("posts/<str:slug>/", views.view_post, name="rcOrH")
]